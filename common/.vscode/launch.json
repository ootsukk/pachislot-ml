{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python デバッガー: ${workspaceFolderBasename:00_Common}",
            "type": "debugpy",
            "request": "launch",
            "program": "${file}",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder:00_Common}",
        }
    ]
}